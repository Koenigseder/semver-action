name: Create release

on: pull_request

jobs:
  create-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: .
        with:
          github-token: ${{ github.token }}
          base-branch: develop
